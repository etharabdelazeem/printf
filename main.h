#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>

int printc(char c);
int _printf(const char *format, ...);
int prints(char *string);
void printd(int i);
int no_of_digits(int i);
int _strlen(char *s);

#endif
